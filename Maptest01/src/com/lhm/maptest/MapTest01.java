package com.lhm.maptest;


/**
 *  Map<K,V>接口常用方法
 *  V put(K key,V value)向Map集合中添加键值对
 *  void clear();清空Map集合
 *  boolean containKey(Object key)判断Map中是否包含某个key
 *  boolean containValue(Object value)判断Map中是否包含某个value
 *  V get(object key);根据key值取value
 *  boolean isEmpty();
 *  Set<K> keySet()获取map集合中所有key，所有的key是一个set集合
 *  V put(K key,V value);
 *  V remove(Object key)通过key删除键值对
 *  int size();获取Map集合中键值对的个数
 *  Collection<V>values();获取map集合中的所有value，返回一个collection
 *  Set<Map.Entry<K,V>>entrySet();将map集合转换成set集合 Map.Entry就是一个类名，他是Map底下的一个静态内部类
 *
 */
public class MapTest01 {
}
